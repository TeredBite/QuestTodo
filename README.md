# Quest To-Do

Приложение, которое превращает твои повседневные дела в эпичные квесты с помощью ИИ. Запиши задачу — получи сюжет, награды и опыт в навыки.

> **.NET MAUI** | **C#** | **OpenRouter API**

---

## Возможности

- **Генерация квестов** — ИИ создаёт эпичное описание задания, ставки и награду на основе твоего дела и заметок
- **Автоматическое определение** — ИИ сам выбирает сложность (1–3), подходящие навыки и количество опыта (10–100 XP)
- **Прокачка навыков** — выполняй задачи, получай опыт и повышай уровень скиллов
- **Кампании** — настрой мир (сеттинг), цель героя и настроение квестов (8 вариантов: серьёзное, юмористическое, тёмное, эпическое и др.)
- **Авторизация и регистрация** — безопасный вход с хешированием паролей (SHA256) и сохранением сессии
- **Отчёты** — три типа: по одному событию, по всем событиям, статистический отчёт с JSON-экспортом
- **Обработка ошибок** — пользовательские подсказки при неверных действиях и уведомления при runtime-ошибках
- **Полностью на русском** — весь интерфейс и тексты квестов на русском языке

---

## Технологии

| Компонент | Технология |
|-----------|------------|
| Фреймворк | .NET 9 MAUI |
| Архитектура | MVVM (CommunityToolkit.Mvvm) |
| ИИ | OpenRouter API (GPT-OSS 120B Free) |
| UI | XAML |
| Платформы | Android, iOS, Windows, macOS |

---

## Установка

### Требования

- [.NET 9 SDK](https://dotnet.microsoft.com/download)
- [Visual Studio 2022](https://visualstudio.microsoft.com/) с workload `.NET MAUI`
- OpenRouter API ключ ([получить](https://openrouter.ai/keys))

### Сборка

```bash
git clone <репозиторий>
cd QuestTodo
dotnet restore
dotnet build
```

### Запуск

```bash
# Windows
dotnet run -f net9.0-windows10.0.19041.0

# Android (эмулятор или устройство)
dotnet run -f net9.0-android

# iOS (требуется macOS)
dotnet run -f net9.0-ios
```

---

## Настройка API ключа

Ключ OpenRouter уже встроен в `Services/OpenRouterQuestGeneratorService.cs`. Для продакшена рекомендуется вынести его в защищённое хранилище:

```csharp
// Services/OpenRouterQuestGeneratorService.cs
private const string ApiKey = "sk-or-v1-ВАШ_КЛЮЧ";
```

---

## Структура проекта

```
QuestTodo/
├── Models/                          # Модели данных
│   ├── Campaign.cs                  # Кампания (мир, цель, настроение)
│   ├── TodoTask.cs                  # Задача / квест
│   ├── Skill.cs                     # Навык игрока
│   ├── QuestResult.cs              # Результат генерации ИИ
│   ├── User.cs                     # Пользователь (Id, логин, хеш пароля)
│   └── AppData.cs                  # Контейнер для сериализации состояния
│
├── Services/                        # Бизнес-логика
│   ├── IQuestGeneratorService.cs   # Интерфейс генератора квестов
│   ├── OpenRouterQuestGeneratorService.cs  # Генерация через OpenRouter
│   ├── LocalQuestGeneratorService.cs # Локальная заглушка
│   ├── ISkillService.cs            # Интерфейс системы опыта
│   ├── SkillService.cs             # Расчёт и добавление XP
│   ├── IAuthService.cs             # Интерфейс авторизации
│   ├── AuthService.cs              # Регистрация, вход, хеширование SHA256
│   ├── IDataExportService.cs       # Интерфейс JSON-экспорта отчётов
│   ├── DataExportService.cs        # Экспорт отчётов в JSON
│   ├── IErrorHandler.cs            # Интерфейс обработки ошибок
│   ├── ErrorHandlerService.cs      # Пользовательские подсказки и runtime-алерты
│   ├── IAppDataStore.cs            # Интерфейс хранилища
│   ├── JsonFileAppDataStore.cs     # JSON-хранилище данных
│   └── AppState.cs                 # Состояние приложения (Observable)
│
├── ViewModels/                      # MVVM ViewModels
│   ├── TasksViewModel.cs           # Список задач
│   ├── TaskEditViewModel.cs        # Создание/редактирование задачи
│   ├── SkillsViewModel.cs          # Список навыков
│   ├── CampaignViewModel.cs        # Настройки кампании
│   ├── LoginViewModel.cs           # Авторизация / регистрация
│   ├── ProfileViewModel.cs         # Профиль пользователя и выход
│   └── ReportsViewModel.cs         # Генерация и экспорт отчётов
│
├── Views/                           # XAML страницы
│   ├── TasksPage.xaml              # Мои дела
│   ├── TaskEditPage.xaml           # Редактор задачи
│   ├── SkillsPage.xaml             # Мои навыки
│   ├── CampaignPage.xaml           # Настройки кампании
│   ├── LoginPage.xaml              # Экран входа
│   ├── ProfilePage.xaml            # Профиль и статистика
│   └── ReportsPage.xaml            # Отчёты и JSON-экспорт
│
├── Converters/                      # XAML конвертеры
│   ├── StringNotNullOrEmptyConverter.cs
│   └── InverseBoolConverter.cs
│
├── AppShell.xaml                    # Навигация (табы + ShellContent)
└── QuestTodoMaui.csproj             # Проект .NET MAUI
```

---

## Как это работает

1. **Регистрация / вход** — создаёшь аккаунт или входишь в существующий (пароли хешируются SHA256)
2. **Создаёшь дело** — вводишь название и опциональные заметки
3. **Генерируешь квест** — ИИ придумывает:
   - Заголовок квеста
   - Описание в терминах мира
   - Что будет, если отложить (stakes)
   - Награду в лоре
   - Сложность (⭐ 1–3)
   - Навыки, которые прокачаются
   - Опыт (+10–100 XP)
4. **Сохраняешь** — квест фиксируется, кнопка генерации пропадает
5. **Выполняешь** — нажимаешь «Выполнено», опыт распределяется по навыкам. Выполненные задачи показывают только кнопку удаления
6. **Смотришь отчёты** — в профиле генерируешь отчёт по событию, по всем событиям или статистику, экспортируешь в JSON
7. **Ошибки обрабатываются** — при неверном вводе получаешь подсказку, при сбое — понятное уведомление

---

## Настроение квестов

| Ключ | Название | Описание |
|------|----------|----------|
| `serious` | Серьёзное | Эпичные и драматичные квесты |
| `humorous` | Юмористическое | Лёгкие и забавные приключения |
| `dark` | Тёмное | Мрачная фэнтези, опасности и жертвы |
| `epic` | Эпическое | Судьбоносные решения, величие и героизм |
| `casual` | Повседневное | Уютные, спокойные истории |
| `noir` | Нуар | Интриги, предательства, моральные дилеммы |
| `horror` | Хоррор | Ужасы, выживание, паранормальное |
| `romantic` | Романтическое | Чувства, отношения, личный рост |

---

## Лицензия

MIT